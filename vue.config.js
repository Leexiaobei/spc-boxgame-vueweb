'use strict'
// Template version: 1.3.1
// see http://vuejs-templates.github.io/webpack for documentation.
const path = require('path')

module.exports = {
    // options...
    // baseUrl: process.env.NODE_ENV === 'TEST' ? './' : '/static/',
    productionSourceMap: false
}